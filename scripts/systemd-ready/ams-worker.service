[Unit]
Description=asset-manager worker service
After=network.target

[Service]
Type=simple
User=deploy
WorkingDirectory=/opt/apps/ams/worker
Environment=NODE_ENV=production
ExecStart=/usr/bin/npm run worker
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
